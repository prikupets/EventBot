package ru.granlovestea.handler.builderbymessages;

import ru.granlovestea.handler.Handler;
import ru.granlovestea.update.Update;

public class BuilderByMessagesHandler implements Handler {
    @Override
    public boolean tryHandle(Update update) {
        return false;
    }
}
